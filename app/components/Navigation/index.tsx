import DesktopNavigation from './Desktop';
import MobileNavigation from './Mobile';

const Navigation = () => {
  return (
    <header>
      <DesktopNavigation />
      <MobileNavigation />
    </header>
  );
};

export default Navigation;
